[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
markers =
    slow: builds real (timm/peft) architectures on CPU — heavier but runnable
    download: requires fetching pretrained weights over the network
    gpu: requires a CUDA device
